[題目]
4.1 Given parent-child collections, identify which relationships should be embedded and which should be linked 在考慮一對多（One-to-Many）關係時，下列哪種情況最適合使用 內嵌（Embedding） 而非引用（Linking）？

[選項]
A. The related data is frequently updated while the parent document remains static / 關聯數據頻繁更新，但父文檔保持不變
B. The child documents are large and grow indefinitely / 子文檔體積巨大且會無限增長
C. The child documents are frequently read together with the parent document / 子文檔經常與父文檔一起被讀取
D. The related data needs to be shared across multiple parent collections / 關聯數據需要被多個父集合共享

[正確答案]：
C

[核心考點]：

[詳細解析]：
當子文件常與父文件一起讀取時，內嵌可大幅減少查詢次數，實現單次 I/O 讀取全套資料。

A. 錯誤，頻繁更新內嵌資料會導致文件不斷重新分配空間，降低效能。
B. 錯誤，無限增長會觸發 16MB 文件上限，應改用 Linking。
C. 正確，符合內嵌的核心優勢，提升讀取效率。
D. 錯誤，需跨集合共享的資料應採用 Linking 以避免資料不一致。

---

[題目]
4.2 Identify anti-pattern data modeling examples 在 MongoDB 中，「無限制增長的陣列（Unbounded Arrays）」被視為一種反模式（Anti-pattern），這主要是為了避免什麼問題？

[選項]
A. Exceeding the 16MB BSON document size limit / 超過 16MB 的 BSON 文檔大小限制
B. Slowing down collection scans / 減慢全表掃描速度
C. Automatic index deletion / 索引自動刪除
D. Replication lag / 副本集同步延遲

[正確答案]：
A

[核心考點]：

[詳細解析]：
無限制增長的陣列會使單一文件無限膨脹，最終突破 MongoDB 單一文件 16MB 的上限。

A. 正確，這是無限制增長陣列最嚴重的反模式風險。
B. 錯誤，全表掃描主要受文件總數與索引影響，非陣列反模式主因。
C. 錯誤，陣列增長不會導致索引被自動刪除。
D. 錯誤，雖然會增加寫入壓力，但非該反模式的主要設計警示。

---

[題目]
5.1 Given a scenario, load the Atlas Sample Dataset and use Data Explorer to find the first document in a collection 在 MongoDB Atlas 控制台中，若要載入官方提供的測試數據（如 sample_mflix 或 sample_weatherdata），應在哪個功能選項下操作？

[選項]
A. Database Access / 資料庫訪問控制
B. Network Access / 網路訪問控制
C. "..." (Ellipsis) button on the Cluster view -> "Load Sample Dataset" / 集群視圖中的 "..." 按鈕 -> "Load Sample Dataset"
D. Command Line Interface (mongosh) / 命令行介面

[正確答案]：
C

[核心考點]：

[詳細解析]：
Atlas 介面在 Cluster 管理視圖中提供快捷按鈕，點擊省略號圖示即可直接匯入測試資料集。

A. 錯誤，此處用於管理資料庫使用者權限。
B. 錯誤，此處用於設定 IP 白名單與網路防火牆。
C. 正確，為 Atlas UI 載入範例資料集的正確入口。
D. 錯誤，mongosh 為 CLI 工具，而非 Atlas UI 的按鈕操作。

---

[題目]
5.1.1 (Follow-up) Identification in Data Explorer 在使用 Atlas Data Explorer 瀏覽集合時，如果沒有設置任何 Filter，顯示的第一個文檔（First Document）通常是基於什麼排序？

[選項]
A. Natural order (order on disk) / 自然順序（磁碟存儲順序）
B. Alphabetical order of the _id / _id 的字母順序
C. Newest document first / 最新建立的文檔優先
D. Sorted by the first field defined / 依據第一個定義的欄位排序

[正確答案]：
A

[核心考點]：

[詳細解析]：
未指定 sort 條件時，MongoDB 預設以磁碟上的實體儲存順序（Natural Order）傳回資料。

A. 正確，無條件查詢時一律依據自然順序回傳。
B. 錯誤，預設不會自動對 _id 進行字母排序。
C. 錯誤，非預設行為，需搭配時間欄位排序。
D. 錯誤，MongoDB 不會根據定義的第一個欄位排序。
